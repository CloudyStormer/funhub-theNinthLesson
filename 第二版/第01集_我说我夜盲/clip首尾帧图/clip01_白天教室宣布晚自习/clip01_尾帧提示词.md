# Clip 01 尾帧提示词（已停用）

Clip01现已恢复为最初的“静态单首帧”方案。本Clip只上传并使用 `clip01_首帧.png`，老师在10秒内始终留在讲台讲话，不生成、不上传、不匹配任何尾帧。

现存 `clip01_尾帧.png` 按用户要求原样保留，不删除、不修改，但它只是历史素材，不是当前生产输入，也不再等同于Clip02首帧。

Clip02使用自己目录中的 `clip02_首帧.png` 与 `clip02_尾帧.png`，通过正常硬切进入老师已经位于右侧过道的稍后时刻。实际生成请返回使用 [`clip01_视频提示词.md`](clip01_视频提示词.md)。
